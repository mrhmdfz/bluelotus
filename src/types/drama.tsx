export type Chapter = { chapterIndex: number; chapterName: string };

export interface DramaDetail {
  bookName: string;
  introduction: string;
  cover: string;
  tags: string[];
  playCount: string;
}
